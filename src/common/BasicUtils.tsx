export const hashName = (name: string): any => {
  return name;
}

/**
 *
 * @param nameHash0
 */
export const getAccountMerkleExistProof = (nameHash0: string): any => {

}

/**
 *
 * @param nameHash0
 */
export const getAccountMerkleNonExistProof = (nameHash0: string): any => {

}


export const checkIfExistingName = (name0: string): any => {
  let name = hashName(name0);
  // send to backend api


}
